Run via the following procedure:
  - cd into folder.
  - type "$ make all" to create needed executables.
  - type "$ make clean" to clean non needed or old compilers (.c, .h and anything else related) apart their flex and bison executables.
